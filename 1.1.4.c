# Prompt the user to enter the base and height of the triangle
base = float(input())
height = float(input())

# Calculate the area of the triangle
area = 0.5 * base * height

# Print the area formatted to two decimal places
print(f"{area:.2f}")

